<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administration_west">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Administration_west">
        <activity android:name=".Pages.DetailsProductsActivity"></activity>
        <activity android:name=".Pages.DetailsCompaniesActivity" />
        <activity android:name=".Pages.FinalizarCompraActivity" />
        <activity android:name=".Pages.PagamentoActivity" />
        <activity android:name=".Pages.CompaniesActivity" />
        <activity android:name=".Pages.CartActivity" />
        <activity android:name=".Pages.DepoisCarrinhoActivity" />

        <activity android:name=".Pages.ProductActivity"></activity>
        <activity android:name=".Pages.RegisterActivity"></activity>

        <activity android:name=".Pages.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>