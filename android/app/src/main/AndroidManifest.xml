<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administration_west">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Administration_west">
        <activity android:name=".PagamentoActivity"></activity>
        <activity android:name=".FinalizarCompraActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".DepoisCarrinhoActivity" />
        <activity android:name=".DetailsCompaniesActivity" />
        <activity android:name=".DetailsProductsActivity" />
        <!--
        <activity android:name=".Pages.ProductActivity"></activity>
        <activity android:name=".RegisterActivity"></activity>
        -->
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>